import 'package:flutter/material.dart';

class BodyWorkListMonth extends StatelessWidget {
  const BodyWorkListMonth({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
